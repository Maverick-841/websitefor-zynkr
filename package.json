{
  "name": "zynkr-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:frontend": "npm run dev --prefix frontend",
    "start:admin": "npm run dev --prefix admin",
    "start:backend": "npm start --prefix backend",
    "start:all": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\" \"node backend/server.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
